// Temporary launch gate: only the homepage is public for now. /about, /work, and
// /work/[slug] stay in the codebase but 404 until this is flipped back to true.
export const SECONDARY_PAGES_ENABLED = false;
