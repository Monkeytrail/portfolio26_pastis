# pastis-design-system is installed as a git dependency (see package.json).
# npm disables git-based installs by default as a supply-chain safety
# measure; this repo explicitly trusts that one dependency.
allow-git=all
